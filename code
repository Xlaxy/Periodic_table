#include <iostream>
#include <string>
#include <conio.h>
#include "table.h"

using namespace std;

int main() {
    while (i = 0) {
        int a;
        int i = 0;
        cout << "Welcome to the periodic table datatbase" << endl;
        cout << "Do nyou want to start the program" << endl << "2 - exit";
        cin >> a;
        if (a = 2){i = 1;
        cout << "Thank you for using the program"; }
